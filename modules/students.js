const student = require('./studentConstructor');

const studentsList = [
  new student('Arissa', 5, [10, 4, 6]),
  new student('Luiz', 3, [10, 10, 10]),
  new student('Luisa', 7, [4, 2, 3]),
  new student('Vinicius', 9, [8, 2, 5]),
  new student('Rafael', 1, [10, 8, 9])
]

module.exports = studentsList
